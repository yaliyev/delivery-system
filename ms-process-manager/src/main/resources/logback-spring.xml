<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender with regular pattern -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/ms-process-manager.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/ms-process-manager.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Enable TRACE logging for your packages -->
    <logger name="de.yagub.deliverysystem.msprocessmanager" level="TRACE"/>
    <logger name="de.yagub.deliverysystem.msprocessmanager.config" level="TRACE"/>
    <logger name="de.yagub.deliverysystem.msprocessmanager.controller" level="TRACE"/>
    <logger name="de.yagub.deliverysystem.msuser.controller" level="TRACE"/>
</configuration>